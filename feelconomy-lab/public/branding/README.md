# 학교 로고 넣는 곳

이 폴더에 학교 로고 파일을 `school-logo.png` 라는 이름으로 넣으면 사이트 헤더와
시작 화면에 자동으로 표시됩니다 (코드 수정 불필요).

```
public/branding/school-logo.png
```

- 배경이 투명한 PNG를 권장합니다 (원형 엠블럼이라면 정사각형 캔버스 + 투명 배경).
- 권장 크기: 256×256px 이상 (정사각형).
- 파일이 없어도 사이트는 정상 작동합니다 — 로고 자리에 아무것도 표시되지 않을 뿐입니다.
